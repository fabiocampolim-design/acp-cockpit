# Upstream watch — 2026-09-09

*acp-cockpit 0.5.0; profile `claude`; written by `scripts/watch_upstream.py`.*

## Adapter

- `@agentclientprotocol/claude-agent-acp`: installed 0.75.1, latest 0.75.1 — up to date
- recent versions: 0.73.0 (2026-09-01), 0.74.0 (2026-09-04), 0.75.0 (2026-09-05), 0.75.1 (2026-09-05), 0.75.2-preview.1 (2026-09-09)

## Protocol schema

- pinned schema-v1.21.0, latest schema-v1.21.0 — up to date

## New since the last run

- adapter version 0.75.2-preview.1 (2026-09-09)
- issue #1110 — session/fork no longer returns a live session since v0.71.0; clients that follow the RFD get "Session not found" (updated 2026-09-09) https://github.com/agentclientprotocol/claude-agent-acp/issues/1110
- issue #1107 — Preserve forkSession in newSession and let Claude mint additive fork ids (updated 2026-09-08) https://github.com/agentclientprotocol/claude-agent-acp/issues/1107
- issue #1094 — Restart + session/load replays prior Write history (same toolCallId); stale grant → new effect not observed (updated 2026-09-08) https://github.com/agentclientprotocol/claude-agent-acp/issues/1094

## Open issues (current)

- #1110 session/fork no longer returns a live session since v0.71.0; clients that follow the RFD get "Session not found"
- #1107 Preserve forkSession in newSession and let Claude mint additive fork ids
- #1094 Restart + session/load replays prior Write history (same toolCallId); stale grant → new effect not observed
- #1092 False "Authentication required" when a normal answer contains "Please run /login"
- #1087 providers/set cannot reach a plain Vertex account — baseUrl is mandatory, and setting it makes Claude Code reject current models
- #1077 0.73.0 ExitPlanMode: clear-context accept is unrecoverable via session/load, mode changes are never published, bypassPermissions unreachable
- #1075 In-session /model switch to a new id drops the effort option that ANTHROPIC_MODEL seeds for the same id
- #1033 setSessionConfigOption rejects valid Anthropic model IDs not in supportedModels() picker

## For this project

- ACPUPSTREAM's findings are true of a version: re-check them against the installed adapter before filing anything.
- A new adapter version means: upgrade, `ACP_COCKPIT_CONTRACT=1 python -m pytest tests/contract/ -q`, recreate `~/.acp-cockpit/acp-patched` with `patch_dist.py`, read the drift chip in a real session.
