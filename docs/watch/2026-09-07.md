# Upstream watch — 2026-09-07

*acp-cockpit 0.5.0; profile `claude`; written by `scripts/watch_upstream.py`.*

## Adapter

- `@agentclientprotocol/claude-agent-acp`: installed 0.75.1, latest 0.75.1 — up to date
- recent versions: 0.72.0 (2026-09-01), 0.73.0 (2026-09-01), 0.74.0 (2026-09-04), 0.75.0 (2026-09-05), 0.75.1 (2026-09-05)

## Protocol schema

- pinned schema-v1.21.0, latest schema-v1.21.0 — up to date

## New since the last run

- nothing new

## Open issues (current)

- #1094 Restart + session/load replays a prior Write approval without a generation-bound fence
- #1092 False "Authentication required" when a normal answer contains "Please run /login"
- #1087 providers/set cannot reach a plain Vertex account — baseUrl is mandatory, and setting it makes Claude Code reject current models
- #1077 0.73.0 ExitPlanMode: clear-context accept is unrecoverable via session/load, mode changes are never published, bypassPermissions unreachable
- #1075 In-session /model switch to a new id drops the effort option that ANTHROPIC_MODEL seeds for the same id
- #1033 setSessionConfigOption rejects valid Anthropic model IDs not in supportedModels() picker
- #1074 A sign-in done outside the query process leaves the signed-out failure active, so the next sign-out publishes no login action
- #1073 The typed session failure replaced the JSON-RPC auth_required rejection, so a capable client's auth flow never starts
- #1072 An api_retry with authentication_failed publishes an access warning that outlives the auth_required rejection
- #1069 There's an issue with the selected model (mimo-v2.5-pro). It may not exist or you may not have access to it.
- #1068 Bash permission prompts show the model-written description instead of the command (regression in 0.71.0)
- #1061 Post-cancel stream_event tool starts are forwarded without terminals — ghost open tool_calls poison the next turn (0.70.0)
- #1060 Stale model is reported after changing models with `/model`
- #1056 session/new's _meta.claudeCode.options.model is silently overridden by settings.model

## For this project

- ACPUPSTREAM's findings are true of a version: re-check them against the installed adapter before filing anything.
- A new adapter version means: upgrade, `ACP_COCKPIT_CONTRACT=1 python -m pytest tests/contract/ -q`, recreate `~/.acp-cockpit/acp-patched` with `patch_dist.py`, read the drift chip in a real session.
